<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!--
      USE_EXACT_ALARM é concedida na instalação e não pode ser revogada: é a
      permissão prevista para apps cuja função principal é despertador. Ela vale
      do Android 13 em diante; abaixo disso o par SCHEDULE_EXACT_ALARM cobre o
      mesmo, pedindo ao usuário quando necessário.
    -->
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"
        android:maxSdkVersion="32" />

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- Sobe o volume do canal de alarme quando ele está no zero. -->
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />

    <uses-feature android:name="android.hardware.camera.any" android:required="true" />

    <application
        android:name=".Aplicacao"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/regras_de_backup"
        android:fullBackupContent="@xml/backup"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Levanta"
        tools:targetApi="35">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- Responde ao "definir alarme" do assistente de voz. -->
            <intent-filter>
                <action android:name="android.intent.action.SET_ALARM" />
                <action android:name="android.intent.action.SHOW_ALARMS" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <!--
          As duas telas do disparo são singleTask e excludeFromRecents: não
          podem empilhar cópias a cada quadro nem ficar acessíveis na lista de
          recentes depois de o alarme acabar. showOnLockScreen e turnScreenOn
          são o par que faz a tela acender sozinha às seis da manhã.
        -->
        <activity
            android:name=".ui.TocandoActivity"
            android:exported="false"
            android:launchMode="singleTask"
            android:excludeFromRecents="true"
            android:showOnLockScreen="true"
            android:turnScreenOn="true"
            android:showWhenLocked="true"
            android:screenOrientation="portrait"
            tools:ignore="LockedOrientationActivity,DiscouragedApi"
            android:taskAffinity=".alarme"
            android:theme="@style/Theme.Levanta.Alarme" />

        <activity
            android:name=".ui.TreinoActivity"
            android:exported="false"
            android:launchMode="singleTask"
            android:excludeFromRecents="true"
            android:showOnLockScreen="true"
            android:turnScreenOn="true"
            android:showWhenLocked="true"
            android:screenOrientation="portrait"
            tools:ignore="LockedOrientationActivity,DiscouragedApi"
            android:taskAffinity=".alarme"
            android:theme="@style/Theme.Levanta.Alarme" />

        <service
            android:name=".alarme.ServicoDeAlarme"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback" />

        <receiver
            android:name=".alarme.ReceptorDeAlarme"
            android:exported="false"
            android:directBootAware="true">
            <intent-filter>
                <action android:name="br.com.levanta.DISPARAR" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".alarme.ReceptorDeBoot"
            android:exported="true"
            android:directBootAware="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.TIMEZONE_CHANGED" />
                <action android:name="android.intent.action.TIME_SET" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
