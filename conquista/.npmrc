# Por padrao instalamos so o que o servidor precisa (express + socket.io).
# As dependencias de desenvolvimento (Playwright, ferramentas de mapa) baixariam
# centenas de MB e quebram o build em hospedagens de plano gratuito.
# Para rodar os testes: npm install --include=dev
omit=dev
fund=false
audit=false
